@model IEnumerable<StudioMetrics.Models.Artist>

@{
    ViewData["Title"] = "Index";
}

<h2>List of Artists</h2>

<form>
    <button asp-action="Create" type="submit" class="btn btn-create btn-default">Create New Artist</button>
</form>
<br />

@*An if statement to tell the user to make artists if none exist*@
@if (Model.Count() == 0)
{
    <p>No Artists have been created yet. Please create Artists to attach to your Projects.</p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
    @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <form>
                        <button asp-action="Edit" asp-route-id="@item.ArtistId" type="submit" class="btn btn-edit btn-default">Edit</button> |
                        <button asp-action="Details" asp-route-id="@item.ArtistId" type="submit" class="btn btn-details btn-default">Details</button> |
                        <button asp-action="Delete" asp-route-id="@item.ArtistId" type="submit" class="btn btn-delete btn-default">Delete</button>
                    </form>
                </td>
            </tr>
    }
        </tbody>
    </table>
}
